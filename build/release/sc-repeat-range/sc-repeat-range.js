/*global define,require,console*/
define([],function(){"use strict";var $injector=angular.injector(["ng"]),$compile=$injector.get("$compile");return{name:"scRepeatRange",replace:!0,scope:{from:"=",to:"=",step:"="},link:function(scope,element,attrs){function range(from,to,step){for(var array=[];from+step<=to;)array[array.length]=from+=step;return array}var from=scope.from||0,step=scope.step||1,to=scope.to||attrs.ngRepeatRange,rangeString=range(from,to+1,step).join(",");angular.element(element).attr("ng-repeat","n in ["+rangeString+"]"),angular.element(element).removeAttr("ng-repeat-range"),$compile(element)(scope)}}});