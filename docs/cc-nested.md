## cc-nested

> Code example how to nest different components.

### Usage:

```html
<ccs-nested>
	<ccs-nested-item></ccs-nested-item>
	<ccs-nested-item></ccs-nested-item>
</ccs-nested>
```

results into:

```html
I am outer
	I am inner
	I am inner
(end of outer)
```
